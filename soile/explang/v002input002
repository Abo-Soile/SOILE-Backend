function f(x)
  if var1 then
    # This should be ignored.
    var2 <- 2
  else
    f1([1 3] f2())  # What a shot.
  end
  return 1+(var2)
end

transition
  |-> InitialPhase,
  NumLetPrac1 -> NumLetPrac2,
  NumLetPrac2 -> NumLetTest if (allCorrect)
end

